#ifndef COLOR_H
#define COLOR_H

#include <opencv2/core.hpp>

const cv::Scalar RED_LOWER = cv::Scalar(136, 87, 111);
const cv::Scalar RED_HIGHER = cv::Scalar(180, 255, 255);

const cv::Scalar BLUE_LOWER = cv::Scalar(94, 80, 2);
const cv::Scalar BLUE_HIGHER = cv::Scalar(120, 255, 255);

const cv::Scalar GREEN_LOWER = cv::Scalar(25, 52, 72);
const cv::Scalar GREEN_HIGHER = cv::Scalar(102, 255, 255);

#endif // !FIELDS_H
